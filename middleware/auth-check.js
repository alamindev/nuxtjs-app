export default function({ store, redirect, route }) {
  // if (!store.state.userToken && route.name !== 'index') {
  //   redirect('/')
  // } else if (store.state.userToken != null && route.name === 'index') {
  //   redirect('/overview')
  // }
}
